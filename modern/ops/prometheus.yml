global:
  scrape_interval: 15s

scrape_configs:
  - job_name: liferpg-backend
    static_configs:
      - targets: ["localhost:8000"]
        labels:
          service: backend
          env: dev
    metrics_path: /metrics
